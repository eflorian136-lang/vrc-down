name: Public Download Trigger
on:
  push:
    paths:
      - 'queue.txt'

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Lese URL und starte Download
        run: |
          URL=$(cat queue.txt)
          python3 download_script.py "$URL"
