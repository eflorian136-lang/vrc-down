export default async function handler(req, res) { const { url } = req.query; if (!url) return res.status(400).send("Keine URL"); res.redirect(`https://loader.to/api/button/?url=${encodeURIComponent(url)}`); }
